{
    "request": {
        "method": "POST",
        "url": "/login",
        "bodyPatterns": [{
		      "matchesJsonPath" : "$[?(@.name == \"invalid_user\")]"
		    }]
    },
    "response": {
        "status": 200,
        "body": "Error"
    }
}